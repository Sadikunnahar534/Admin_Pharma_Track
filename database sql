CREATE DATABASE IF NOT EXISTS pharmatrack
CHARACTER SET utf8
COLLATE utf8_general_ci;

USE pharmatrack;


CREATE TABLE IF NOT EXISTS categories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
INSERT INTO categories (name, description) VALUES
('Antibiotics', 'Medicines that kill or inhibit bacteria'),
('Analgesics', 'Pain relief medicines'),
('Antacids', 'Medicines for acidity and heartburn'),
('Vitamins & Supplements', 'Vitamins, minerals and supplements'),
('Antidiabetics', 'Medicines for diabetes management'),
('Antihistamines', 'Allergy relief medicines'),
('Cardiovascular', 'Heart and blood pressure medicines'),
('Dermatology', 'Skin care medicines and creams');







-- Medicines Table
CREATE TABLE IF NOT EXISTS medicines (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    generic_name VARCHAR(200),
    category_id INT,
    manufacturer VARCHAR(150),
    unit VARCHAR(50) DEFAULT 'pcs',
    purchase_price DECIMAL(10,2) NOT NULL DEFAULT 0,
    selling_price DECIMAL(10,2) NOT NULL DEFAULT 0,
    stock_quantity INT NOT NULL DEFAULT 0,
    min_stock_level INT DEFAULT 10,
    expiry_date DATE,
    batch_number VARCHAR(100),
    description TEXT,
    status ENUM('active','inactive') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    ON UPDATE CURRENT_TIMESTAMP,

    FOREIGN KEY (category_id)
    REFERENCES categories(id)
    ON DELETE SET NULL
);

-- Insert Sample Medicines
INSERT INTO medicines
(name, generic_name, category_id, manufacturer, unit,
purchase_price, selling_price, stock_quantity,
min_stock_level, expiry_date, batch_number)

VALUES
('Napa 500mg', 'Paracetamol', 2, 'Beximco Pharma',
'pcs', 0.50, 1.00, 500, 50, '2026-12-31', 'BX2024001'),

('Napa Extra', 'Paracetamol + Caffeine', 2,
'Beximco Pharma', 'pcs', 0.80, 1.50, 300, 30,
'2026-06-30', 'BX2024002'),

('Amoxil 500mg', 'Amoxicillin', 1,
'GlaxoSmithKline', 'pcs', 8.00, 15.00,
200, 20, '2025-09-30', 'GSK2024001'),

('Seclo 20mg', 'Omeprazole', 3,
'Square Pharma', 'pcs', 3.50, 7.00,
150, 15, '2026-03-31', 'SQ2024001');
































































CREATE TABLE IF NOT EXISTS sale_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    sale_id INT NOT NULL,
    medicine_id INT NOT NULL,
    quantity INT NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL,
    total_price DECIMAL(10,2) NOT NULL,

    FOREIGN KEY (sale_id)
    REFERENCES sales(id)
    ON DELETE CASCADE,

    FOREIGN KEY (medicine_id)
    REFERENCES medicines(id)
    ON DELETE RESTRICT
);


CREATE TABLE IF NOT EXISTS stock_adjustments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    medicine_id INT NOT NULL,
    adjustment_type ENUM
    ('purchase','return','damage','correction')
    NOT NULL,

    quantity INT NOT NULL,
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (medicine_id)
    REFERENCES medicines(id)
    ON DELETE CASCADE
);


INSERT INTO sale_items
(sale_id, medicine_id, quantity,
unit_price, total_price)

VALUES
(1, 1, 50, 1.00, 50.00),
(1, 4, 10, 7.00, 70.00),
(1, 10, 15, 2.00, 30.00),
(2, 3, 10, 15.00, 150.00),
(2, 6, 20, 3.00, 60.00);



-- Additional Medicines
INSERT INTO medicines
(name, generic_name, category_id, manufacturer,
unit, purchase_price, selling_price,
stock_quantity, min_stock_level,
expiry_date, batch_number)

VALUES
('A-Zinc', 'Zinc Sulfate', 4,
'ACI Limited', 'pcs',
2.00, 4.00, 400, 40,
'2027-01-31', 'ACI2024001'),

('Vitamin C 500mg', 'Ascorbic Acid', 4,
'Drug International', 'pcs',
1.00, 2.00, 600, 60,
'2027-06-30', 'DI2024001');

-- Testing Queries
SELECT * FROM categories;

SELECT * FROM medicines;

SELECT * FROM sales;

SELECT * FROM sale_items;

-- Low Stock Medicines
SELECT name, stock_quantity
FROM medicines
WHERE stock_quantity < min_stock_level;

-- Expired Medicines
SELECT name, expiry_date
FROM medicines
WHERE expiry_date < CURDATE();
