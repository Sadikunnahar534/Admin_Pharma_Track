Create Database

CREATE DATABASE pharma_track;
USE pharma_track;


CREATE TABLE manufacturer (
    manufacturer_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    contact VARCHAR(50),
    address VARCHAR(255)
);



CREATE TABLE drug (
    drug_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    category VARCHAR(50),
    manufacturer_id INT,
    price DECIMAL(10,2),
    FOREIGN KEY (manufacturer_id) REFERENCES manufacturer(manufacturer_id)
);


CREATE TABLE batch (
    batch_id INT AUTO_INCREMENT PRIMARY KEY,
    drug_id INT,
    batch_number VARCHAR(100) UNIQUE,
    manufacture_date DATE,
    expiry_date DATE,
    qr_code VARCHAR(255),
    FOREIGN KEY (drug_id) REFERENCES drug(drug_id)
);



CREATE TABLE inventory (
    inventory_id INT AUTO_INCREMENT PRIMARY KEY,
    drug_id INT,
    quantity INT,
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (drug_id) REFERENCES drug(drug_id)
);



CREATE TABLE customer (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    phone VARCHAR(20),
    address VARCHAR(255)
);


CREATE TABLE sales (
    sale_id INT AUTO_INCREMENT PRIMARY KEY,
    drug_id INT,
    customer_id INT,
    quantity INT,
    total_price DECIMAL(10,2),
    sale_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (drug_id) REFERENCES drug(drug_id),
    FOREIGN KEY (customer_id) REFERENCES customer(customer_id)
);



Manufacturer
INSERT INTO manufacturer (name, contact, address)
VALUES ('Square Pharma', '01711111111', 'Dhaka');

Drug
INSERT INTO drug (name, category, manufacturer_id, price)
VALUES ('Napa', 'Painkiller', 1, 2.50);

Batch
INSERT INTO batch (drug_id, batch_number, manufacture_date, expiry_date, qr_code)
VALUES (1, 'BATCH001', '2025-01-01', '2027-01-01', 'QR123456');

Inventory
INSERT INTO inventory (drug_id, quantity)
VALUES (1, 1000);

Customer
INSERT INTO customer (name, phone, address)
VALUES ('Rahim', '01812345678', 'Dhaka');

Sales
INSERT INTO sales (drug_id, customer_id, quantity, total_price)
VALUES (1, 1, 5, 12.50);



Search Drug
SELECT * FROM drug WHERE name LIKE '%Napa%';
 Search Customer
SELECT * FROM customer WHERE name LIKE '%Rahim%';

Sales Report
SELECT d.name, c.name AS customer, s.quantity, s.total_price
FROM sales s
JOIN drug d ON s.drug_id = d.drug_id
JOIN customer c ON s.customer_id = c.customer_id;
Update Stock
UPDATE inventory SET quantity = 900 WHERE drug_id = 1;

Update Price
UPDATE drug SET price = 3.00 WHERE drug_id = 1;

Update Customer Info
UPDATE customer SET phone = '01999999999' WHERE customer_id = 1;




Delete Drug
DELETE FROM drug WHERE drug_id = 1;

 Delete Customer
DELETE FROM customer WHERE customer_id = 1;

 Admin View Query
SELECT * FROM drug;
SELECT * FROM customer;
SELECT * FROM sales;


